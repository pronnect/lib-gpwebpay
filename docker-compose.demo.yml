services:
  php:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8080:8080"
    command: ash -c "composer install --no-suggest && php -S 0.0.0.0:8080 -t demo demo/router.php"